# hackathon-sitespeak-ai
A "Zero-UI" web app (practically just one big button) designed specifically for mobile devices. The site worker presses the button (or takes a photo of the damage), speaks naturally, and the AI automatically converts the unstructured voice input into a structured order/task sent directly to the foreman (on Trello).
